// Exportar el hook usePageTitle
export { usePageTitle } from '@/context/usePageTitle';

// Exportar otros contextos existentes
export { AuthProvider, useAuth, type MeData } from '@/context/AuthContext';
export { useUserInfo, type UserType, type AllUser, type UserInfo } from '@/context/useUserInfo'; 